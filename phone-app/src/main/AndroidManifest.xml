<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <permission
        android:name="actor.starintel.permission.CONFIG"
        android:protectionLevel="signature" />
    <permission
        android:name="actor.starintel.permission.HACKMODE_OPERATIONS"
        android:protectionLevel="signature" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-permission android:name="actor.starintel.permission.CONFIG" />

    <application
        android:allowBackup="false"
        android:label="StarIntel Companion"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <provider
            android:name=".StarIntelConfigProvider"
            android:authorities="actor.starintel.config"
            android:exported="true"
            android:readPermission="actor.starintel.permission.CONFIG"
            android:writePermission="actor.starintel.permission.CONFIG" />
        <activity android:name="actor.starintel.update.UpdateInstallResultActivity" android:exported="false" />
        <activity android:name=".PhonePackagesActivity" android:exported="false" />
        <activity android:name=".UpdateManagerActivity" android:exported="false" />
        <activity android:name=".WatchPackagesActivity" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
