<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:label="Quasar"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".ide.LogicStudioActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".workflows.WorkflowStatusActivity"
            android:exported="false"
            android:label="@string/workflow_status_title" />
        <activity
            android:name=".workflows.FlowStudioActivity"
            android:exported="false"
            android:label="Flow studio" />

        <receiver
            android:name=".workflows.WorkflowWidgetActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".workflows.WorkflowWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/workflow_widget_info" />
        </receiver>
    </application>
</manifest>
